---
id: a751284f-be2f-4c6d-8528-c39ca9940beb
type: daily-log
title: 2026-07-12
timestamp: 2026-07-12 19:03:06
---

# 2026-07-12

## 19:03:06

### Concepts

- Evaluated the two Cognosis docs in [workspace/cognosis-docs](../../workspace/cognosis-docs/): the architecture draft and implementation guide are unusually decision-complete — 9 of 11 open questions resolved with reasoning, artifact-based exit criteria per phase, dependency ledger with per-import justification.
- Strongest sections worth reusing as patterns: the hybrid embedding migration (fallback view + backfill + lazy touch-migration, idempotent by construction) and two-tier persona discovery (context-window cost as a first-class design constraint).

### Cursed Knowledge

- Cognosis drops silo-kb's git recovery net without replacing it — the vault has no version history at all, which makes bad compile runs unrecoverable and silently guts the `as_of` temporal-query feature (old note content ceases to exist on update).
- The §4.2 rationale for mirroring `notes.content` in Postgres ("Cognosis is the sole writer") is invalidated one section earlier by §4.1's Obsidian escape hatch — the mirror's integrity actually rests on §4.1.1 reconciliation.
- The fail-loud SessionStart hook as specified would block *every* Claude Code session on the machine when the daemon is down, including repos unrelated to Cognosis — nothing says the hook no-ops without a `.cognosis-project` marker.

### Unresolved

- [ ] Add vault version history to the Cognosis design (auto-managed local git?) — fixes both recoverability and the `as_of` overpromise
- [ ] Make the SessionStart hook conditional on the `.cognosis-project` marker (exit 0 silently when absent)
- [ ] Fix §4.2's "sole writer" rationale to cite reconciliation instead
- [ ] Specify the periodic-sweep interval and document the watcher-suppression drift window (external edit during an active MCP write is lost until the sweep)
- [ ] Extend §4.6 hard-delete cascade to cover `log.md` (and acknowledge Postgres WAL/backups)
- [ ] State that loopback-only bind is in place before Phase 3b's auth-pass-through MCP server ships

### Log

- Read and evaluated `cognosis-architecture-draft.md` and `cognosis-implementation-guide.md`; delivered full evaluation in chat, captured findings here.

## 19:12:47

### Concepts

- Operator ruled on the vault-history gap: git over frontmatter. Frontmatter was rejected because history requires retaining prior note bodies — frontmatter carrying its own file's past bodies is a reinvented VCS, not metadata. Design now has §4.7: auto-managed, local-only git repo in the vault (recovery net, not staleness signal), one commit per sanctioned write, one per compile run, drift committed as found.
- Hard-delete now cascades to `log.md` (tombstone rewrite — the one sanctioned exception to append-only) and vault git history (filter-repo-style purge); Postgres WAL/backups named as operator-owned rather than ignored.

### Log

- Amended both Cognosis docs per operator direction: added §4.7 vault history (git), gated the SessionStart hook on the `.cognosis-project` marker (exit 0 when absent), fixed §4.2's "sole writer" rationale to cite reconciliation, specified the 60-min periodic sweep + documented the watcher-suppression drift window, extended §4.6 erasure cascade, stated Phase 3b's loopback-bind-before-auth ordering, and threaded §4.7 through the implementation guide's phases and dependency ledger. All six evaluation gaps from the 19:03 block are now resolved in the docs.
